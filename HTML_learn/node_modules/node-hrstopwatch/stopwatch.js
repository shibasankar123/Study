"use strict";

class Stopwatch {
    start() {
        this.startTime = process.hrtime();
    }

    stop() {
        this.stopTime = process.hrtime(this.startTime);
    }

    getDuration() {
        return this.stopTime[0] * 1e9 + this.stopTime[1];
    }
}

module.exports = Stopwatch;
